defaults:
  - _base

exp_name:        train_with_no_prompt_old
train:           1

use_prompts:     0          # same as base but explicit

batch_size:      5
epochs:          6
save_epochs:     1
eval_epochs:     1