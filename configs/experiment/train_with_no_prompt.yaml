defaults:
  - _base

exp_name:        train_with_no_prompt
train:           1

use_prompts:     0          # same as base but explicit

batch_size:      1
epochs:          6
save_epochs:     1
eval_epochs:     1